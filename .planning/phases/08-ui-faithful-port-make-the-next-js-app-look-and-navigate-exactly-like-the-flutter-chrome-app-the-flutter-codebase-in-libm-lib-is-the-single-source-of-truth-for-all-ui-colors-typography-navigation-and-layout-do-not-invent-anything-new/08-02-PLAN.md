---
phase: 08-ui-faithful-port
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/components/bookshelf/BookSpine.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Book spines are 56px wide × 200px tall (not 150px)"
    - "Spine title text is 14px Georgia (not 16px)"
    - "Spine author text is 14px Georgia (not 13px)"
    - "Text wrapper maxHeight accommodates 200px spine (≥190px)"
    - "The author span has no opacity reduction (Flutter renders author at full opacity)"
  artifacts:
    - path: "web/components/bookshelf/BookSpine.tsx"
      provides: "Corrected BookSpine: 56×200px, 14px+14px text"
      contains: "height: '200px'"
  key_links:
    - from: "web/components/bookshelf/BookSpine.tsx"
      to: "web/components/bookshelf/LibraryShelf.tsx"
      via: "rendered inside SortableBookSpine"
      pattern: "<BookSpine"
    - from: "web/components/bookshelf/BookSpine.tsx"
      to: "web/components/bookshelf/Bookshelf.tsx"
      via: "rendered for each book in static shelf"
      pattern: "<BookSpine"
---

<objective>
Correct BookSpine.tsx to match the Flutter BookSpineWidget.dart exactly: 56×200px dimensions and 14px font size for both title and author.

Purpose: Book spines are currently 56×150px with 16px+13px text — mismatched from Flutter's 56×200px with 14px+14px text. This is the most visually obvious parity gap.
Output: BookSpine.tsx with corrected height and font sizes.
</objective>

<execution_context>
@/Users/anish/Developer/Personal/LibM/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anish/Developer/Personal/LibM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-ui-faithful-port-make-the-next-js-app-look-and-navigate-exactly-like-the-flutter-chrome-app-the-flutter-codebase-in-libm-lib-is-the-single-source-of-truth-for-all-ui-colors-typography-navigation-and-layout-do-not-invent-anything-new/08-RESEARCH.md

<interfaces>
<!-- Flutter source: libm/lib/features/library/widgets/book_spine_widget.dart -->
<!-- Verified dimensions from RESEARCH.md -->

BookSpineWidget dimensions:
  width:  56px (unchanged — already correct)
  height: 200px (currently 150px — WRONG)

BookSpineWidget typography (AppTextStyles.spineTitle + spineAuthor):
  title:  Georgia, 14px, normal weight, maxLines 2, ellipsis overflow
  author: Georgia, 14px, normal weight, maxLines 1, ellipsis overflow
  spacing between title and author: 4px (gap: '4px' — already correct)
  padding inside spine: 8px horizontal (already correct)

Flutter rotation: RotatedBox(quarterTurns: 1) = 90° clockwise
Next.js equivalent: writing-mode: vertical-rl + transform: rotate(180deg) (already correct — do not change)

Author opacity: Flutter renders author at full opacity (no 0.85 reduction). Remove opacity from author span.

Text wrapper maxHeight: currently '140px' — must be updated to accommodate 200px spine.
New maxHeight should be '190px' (200px height - 2×border - small buffer).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BookSpine.tsx — height 200px, both font sizes 14px, remove author opacity</name>
  <files>web/components/bookshelf/BookSpine.tsx</files>
  <read_first>
    - web/components/bookshelf/BookSpine.tsx (current state — see all inline style values)
    - libm/lib/features/library/widgets/book_spine_widget.dart (Flutter source of truth)
  </read_first>
  <action>
    Make the following targeted changes to web/components/bookshelf/BookSpine.tsx:

    1. Change spine height from 150px to 200px:
       CHANGE: style={{ width: '56px', height: '150px', backgroundColor: coverColor }}
       TO:     style={{ width: '56px', height: '200px', backgroundColor: coverColor }}

    2. Change the rotated text wrapper maxHeight from 140px to 190px:
       CHANGE: maxHeight: '140px'
       TO:     maxHeight: '190px'

    3. Change title font size from 16px to 14px:
       CHANGE: fontSize: '16px'  (on the title span)
       TO:     fontSize: '14px'

    4. Change author font size from 13px to 14px:
       CHANGE: fontSize: '13px'  (on the author span)
       TO:     fontSize: '14px'

    5. Remove author opacity (Flutter renders at full opacity):
       REMOVE: opacity: 0.85  (from the author span style)

    Do NOT change: width (56px), border (2px border-black), writing-mode, transform, gap (4px), padding logic, getContrastColor function, or any TypeScript interface.
  </action>
  <verify>
    <automated>grep -E "(height:|fontSize:|opacity)" /Users/anish/Developer/Personal/LibM/web/components/bookshelf/BookSpine.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BookSpine.tsx contains `height: '200px'`
    - BookSpine.tsx does NOT contain `height: '150px'`
    - BookSpine.tsx contains `maxHeight: '190px'`
    - BookSpine.tsx does NOT contain `maxHeight: '140px'`
    - BookSpine.tsx title span contains `fontSize: '14px'` (the first fontSize in the rotated wrapper)
    - BookSpine.tsx does NOT contain `fontSize: '16px'` (old title size)
    - BookSpine.tsx author span contains `fontSize: '14px'` (both font sizes are now '14px')
    - BookSpine.tsx does NOT contain `fontSize: '13px'` (old author size)
    - BookSpine.tsx does NOT contain `opacity: 0.85` (removed from author span)
  </acceptance_criteria>
  <done>BookSpine.tsx renders 56×200px spines with 14px title and 14px author at full opacity — exactly matching Flutter BookSpineWidget.dart.</done>
</task>

</tasks>

<verification>
Run after task completes:

```bash
# Confirm all dimension and font changes
grep -n "height\|fontSize\|opacity\|maxHeight" /Users/anish/Developer/Personal/LibM/web/components/bookshelf/BookSpine.tsx

# Expected output contains:
#   height: '200px'         (not 150px)
#   maxHeight: '190px'      (not 140px)
#   fontSize: '14px'        (twice — title and author)
#   NO opacity: 0.85

# TypeScript still compiles
cd /Users/anish/Developer/Personal/LibM/web && npx tsc --noEmit 2>&1 | head -10
```
</verification>

<success_criteria>
- BookSpine.tsx: height='200px', maxHeight='190px', both font sizes '14px', no opacity on author
- No TypeScript errors introduced
- getContrastColor function untouched (still WCAG-compliant)
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-faithful-port-make-the-next-js-app-look-and-navigate-exactly-like-the-flutter-chrome-app-the-flutter-codebase-in-libm-lib-is-the-single-source-of-truth-for-all-ui-colors-typography-navigation-and-layout-do-not-invent-anything-new/08-02-SUMMARY.md`
</output>
