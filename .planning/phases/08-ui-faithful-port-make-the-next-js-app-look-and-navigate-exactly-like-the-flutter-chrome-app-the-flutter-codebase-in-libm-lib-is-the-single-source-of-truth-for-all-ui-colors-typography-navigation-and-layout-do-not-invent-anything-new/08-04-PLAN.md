---
phase: 08-ui-faithful-port
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - web/app/library/page.tsx
  - web/app/library/add/page.tsx
  - web/app/wishlist/add/page.tsx
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "Library page heading is 32px bold Georgia (not 48px)"
    - "Library page add button uses background color #D4AF6A (not #D4AF37)"
    - "Add book (library) page heading is 32px bold Georgia (not 48px)"
    - "Add book (wishlist) page heading is 32px bold Georgia (not 48px)"
    - "Input focus border on add book forms is forest green #2D4A3E (not #D4AF37)"
    - "Selected cover swatch has 3px solid #000000 border (not 2px solid #D4AF37)"
    - "Save Book button background is forest green #2D4A3E with white text (matches Flutter AddBookScreen)"
  artifacts:
    - path: "web/app/library/page.tsx"
      provides: "Corrected Library heading 32px, add button #D4AF6A"
      contains: "fontSize: '32px'"
    - path: "web/app/library/add/page.tsx"
      provides: "Corrected heading 32px, focus border forest-green, swatch border 3px black, Save button forest-green"
      contains: "fontSize: '32px'"
    - path: "web/app/wishlist/add/page.tsx"
      provides: "Same corrections as library/add"
      contains: "fontSize: '32px'"
  key_links:
    - from: "web/app/library/page.tsx"
      to: "heading font size"
      via: "inline style fontSize"
      pattern: "fontSize.*32px"
    - from: "web/app/library/add/page.tsx"
      to: "input onFocus handler"
      via: "onFocus event inline style"
      pattern: "2D4A3E"
---

<objective>
Correct the Library page heading size and add button color, and fix the add book forms (library and wishlist variants) to match Flutter's AddBookScreen exactly: 32px headings, forest-green focus borders, 3px black selected swatch borders, and forest-green Save button.

Purpose: The Library page heading is visually too large (48px vs Flutter's 32px). The add form focus and selection states use the wrong gold (#D4AF37) instead of forest green (focus) and black (selected swatch).
Output: library/page.tsx, library/add/page.tsx, wishlist/add/page.tsx with all UI parity corrections.
</objective>

<execution_context>
@/Users/anish/Developer/Personal/LibM/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anish/Developer/Personal/LibM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-ui-faithful-port-make-the-next-js-app-look-and-navigate-exactly-like-the-flutter-chrome-app-the-flutter-codebase-in-libm-lib-is-the-single-source-of-truth-for-all-ui-colors-typography-navigation-and-layout-do-not-invent-anything-new/08-RESEARCH.md
@.planning/phases/08-ui-faithful-port-make-the-next-js-app-look-and-navigate-exactly-like-the-flutter-chrome-app-the-flutter-codebase-in-libm-lib-is-the-single-source-of-truth-for-all-ui-colors-typography-navigation-and-layout-do-not-invent-anything-new/08-01-SUMMARY.md

<interfaces>
<!-- Flutter source: libm/lib/features/library/library_screen.dart, add_book_screen.dart -->
<!-- Verified values from RESEARCH.md -->

LibraryScreen heading:
  Flutter: AppBar title "Library", style AppTextStyles.heading = 32px bold Georgia
  Next.js: currently fontSize: '48px' — WRONG. Should be '32px'.

LibraryScreen add button ("+"):
  Flutter: themed as Aged Gold. coverSwatches[4] = #D4AF6A
  Next.js: currently backgroundColor: '#D4AF37' — WRONG. Should be '#D4AF6A'.

AddBookScreen heading:
  Flutter: "Add Book", style AppTextStyles.heading = 32px bold Georgia
  Next.js: currently fontSize: '48px' — WRONG. Should be '32px'.

AddBookScreen input focus border:
  Flutter: focusedBorder: OutlineInputBorder(borderSide: BorderSide(color: AppColors.forestGreen, width: 2))
  AppColors.forestGreen = #2D4A3E
  Next.js: currently onFocus sets border to '2px solid #D4AF37' — WRONG. Should be '2px solid #2D4A3E'.

AddBookScreen color picker swatch (selected):
  Flutter ColorPickerWidget: selectedBorder: Border.all(color: Colors.black, width: 3)
  CSS: border: '3px solid #000000'
  Next.js: currently border: coverColor === hex ? '2px solid #D4AF37' : '2px solid #000000' — WRONG.
  Should be: border: coverColor === hex ? '3px solid #000000' : '1px solid rgba(0,0,0,0.1)'
  (Flutter also uses 1px rgba(0,0,0,0.1) for unselected swatches, not 2px black)
  Also remove the outline property on swatches (Flutter does not use outline, only border).

AddBookScreen Save button:
  Flutter: ElevatedButton with color AppColors.forestGreen (#2D4A3E), white text
  Next.js library/add: currently backgroundColor: saving ? 'rgba(212, 175, 55, 0.5)' : '#D4AF37'
  Should be: backgroundColor: saving ? 'rgba(45, 74, 62, 0.5)' : '#2D4A3E', color: '#FFFFFF'
  Note: text color changes to white (contrast on dark green background)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Library page heading size and add button color</name>
  <files>web/app/library/page.tsx</files>
  <read_first>
    - web/app/library/page.tsx (current state — see h1 fontSize and add button backgroundColor)
    - libm/lib/features/library/library_screen.dart (Flutter source of truth)
  </read_first>
  <action>
    In web/app/library/page.tsx, make these changes:

    1. Fix the Library heading font size:
       CHANGE: fontSize: '48px'  (on the h1 element)
       TO:     fontSize: '32px'

    2. Remove letterSpacing from the Library heading (Flutter heading style has no letter spacing; only display/app-name has letterSpacing: 2px):
       REMOVE: letterSpacing: '2px'  (from the h1 inline style)
       Note: The current heading incorrectly inherits display-style letter spacing.

    3. Fix the add button background color:
       CHANGE: backgroundColor: '#D4AF37'
       TO:     backgroundColor: '#D4AF6A'

    Do NOT change: heading fontWeight (700), heading color (#222222), heading lineHeight, button dimensions (48×48px), button border (2px solid #000000), button text ('+'), or anything else.
  </action>
  <verify>
    <automated>grep -E "(fontSize|letterSpacing|backgroundColor)" /Users/anish/Developer/Personal/LibM/web/app/library/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - library/page.tsx h1 contains `fontSize: '32px'`
    - library/page.tsx does NOT contain `fontSize: '48px'` on the h1
    - library/page.tsx does NOT contain `letterSpacing: '2px'` on the h1 (removed — display style letterSpacing is for app title only)
    - library/page.tsx contains `backgroundColor: '#D4AF6A'`
    - library/page.tsx does NOT contain `backgroundColor: '#D4AF37'`
  </acceptance_criteria>
  <done>Library page heading is 32px (matches Flutter LibraryScreen AppBar), add button is Aged Gold #D4AF6A (correct swatch color).</done>
</task>

<task type="auto">
  <name>Task 2: Fix add book form — heading, focus border, swatch selection, Save button (library and wishlist variants)</name>
  <files>web/app/library/add/page.tsx, web/app/wishlist/add/page.tsx</files>
  <read_first>
    - web/app/library/add/page.tsx (current state — see all style values)
    - web/app/wishlist/add/page.tsx (current state — verify it mirrors library/add)
    - libm/lib/features/library/add_book_screen.dart (Flutter source of truth)
  </read_first>
  <action>
    Apply the same four corrections to BOTH web/app/library/add/page.tsx AND web/app/wishlist/add/page.tsx:

    1. Fix "Add Book" heading font size:
       CHANGE: fontSize: '48px'  (on the h1 / page heading element)
       TO:     fontSize: '32px'

    2. Fix input focus border color on ALL input/textarea onFocus handlers:
       CHANGE: e.target.style.border = '2px solid #D4AF37'
       TO:     e.target.style.border = '2px solid #2D4A3E'
       (This applies to: title input onFocus, author input onFocus, notes textarea onFocus)

    3. Fix color picker swatch borders:
       CHANGE: border: coverColor === hex ? '2px solid #D4AF37' : '2px solid #000000'
       TO:     border: coverColor === hex ? '3px solid #000000' : '1px solid rgba(0, 0, 0, 0.1)'
       Also REMOVE the `outline` and `outlineOffset` properties from the swatch button style entirely
       (Flutter uses only border, no CSS outline)

    4. Fix Save button background color and text color:
       CHANGE: backgroundColor: saving ? 'rgba(212, 175, 55, 0.5)' : '#D4AF37'
       TO:     backgroundColor: saving ? 'rgba(45, 74, 62, 0.5)' : '#2D4A3E'
       CHANGE: color: '#222222'  (on the Save button)
       TO:     color: '#FFFFFF'
       (Forest Green background requires white text for WCAG contrast)

    Apply all four corrections to BOTH files. wishlist/add/page.tsx is a near-mirror of library/add/page.tsx — it may already have the same issues.
  </action>
  <verify>
    <automated>grep -E "(fontSize.*32|2D4A3E|3px solid #000|rgba\(45|#FFFFFF|D4AF37|48px|outline)" /Users/anish/Developer/Personal/LibM/web/app/library/add/page.tsx && grep -E "(fontSize.*32|2D4A3E|3px solid #000|rgba\(45|#FFFFFF|D4AF37|48px|outline)" /Users/anish/Developer/Personal/LibM/web/app/wishlist/add/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    Both library/add/page.tsx AND wishlist/add/page.tsx:
    - Contain `fontSize: '32px'` on the page heading
    - Do NOT contain `fontSize: '48px'` on the page heading
    - Contain `'2px solid #2D4A3E'` in onFocus handlers (for input and textarea focus borders)
    - Do NOT contain `'2px solid #D4AF37'` (old wrong focus border color)
    - Contain `'3px solid #000000'` for selected swatch border
    - Contain `'1px solid rgba(0, 0, 0, 0.1)'` for unselected swatch border
    - Do NOT contain `'2px solid #D4AF37'` as swatch selected border
    - Do NOT contain `outline:` on swatch buttons (removed)
    - Contain `backgroundColor: saving ? 'rgba(45, 74, 62, 0.5)' : '#2D4A3E'` on Save button
    - Save button contains `color: '#FFFFFF'`
    - Do NOT contain `rgba(212, 175, 55` (old wrong gold disabled color)
  </acceptance_criteria>
  <done>Both add book forms (library and wishlist variants) exactly match Flutter AddBookScreen: 32px heading, forest-green focus border #2D4A3E, 3px black selected swatch border, forest-green Save button with white text.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
# Confirm no wrong gold color remains in page files
grep -rn "D4AF37" /Users/anish/Developer/Personal/LibM/web/app/
# Expected: no matches (all replaced with #D4AF6A or #2D4A3E)

# Confirm forest green focus borders present
grep -rn "2D4A3E" /Users/anish/Developer/Personal/LibM/web/app/library/add/page.tsx \
  /Users/anish/Developer/Personal/LibM/web/app/wishlist/add/page.tsx
# Expected: appears in both files (onFocus handler)

# Confirm heading sizes corrected
grep -n "32px" /Users/anish/Developer/Personal/LibM/web/app/library/page.tsx \
  /Users/anish/Developer/Personal/LibM/web/app/library/add/page.tsx \
  /Users/anish/Developer/Personal/LibM/web/app/wishlist/add/page.tsx
# Expected: one match per file (the heading h1)

# TypeScript compiles
cd /Users/anish/Developer/Personal/LibM/web && npx tsc --noEmit 2>&1 | head -10
```
</verification>

<success_criteria>
- library/page.tsx: heading 32px (no letterSpacing), add button #D4AF6A
- library/add/page.tsx + wishlist/add/page.tsx: heading 32px, focus border #2D4A3E, selected swatch 3px solid #000000, unselected swatch 1px solid rgba(0,0,0,0.1), no outline on swatches, Save button #2D4A3E with white text
- Zero occurrences of #D4AF37 anywhere in web/app/ directory
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-faithful-port-make-the-next-js-app-look-and-navigate-exactly-like-the-flutter-chrome-app-the-flutter-codebase-in-libm-lib-is-the-single-source-of-truth-for-all-ui-colors-typography-navigation-and-layout-do-not-invent-anything-new/08-04-SUMMARY.md`
</output>
