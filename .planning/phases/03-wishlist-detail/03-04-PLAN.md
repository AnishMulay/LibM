---
phase: 03-wishlist-detail
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified: []
autonomous: false
requirements:
  - WISH-01
  - WISH-02
  - BOOK-01
  - PLAT-01

must_haves:
  truths:
    - "She can add a book to her wishlist on Android and it appears immediately in the wishlist shelf view"
    - "He can see her wishlist on iOS with no add button visible"
    - "He can tap a wishlist book spine, open detail view, and tap 'Move to Library' — the book disappears from the wishlist"
    - "Tapping any shelf book opens detail view showing all its fields (cover, title, author, notes if present)"
    - "Both devices see the same data (cross-device sync works via Supabase)"
  artifacts:
    - path: "libm/lib/features/wishlist/wishlist_screen.dart"
      provides: "Running on both devices"
    - path: "libm/lib/features/library/book_detail_screen.dart"
      provides: "Running on both devices"
  key_links:
    - from: "WishlistScreen"
      to: "Supabase books table"
      via: "fetchWishlist visible on both devices"
      pattern: "is_wishlist = true in DB"
---

<objective>
Human verification that the complete feature works end-to-end on both physical devices.

Purpose: PLAT-01 requires full parity on iOS (his device) and Android (her device). This checkpoint confirms the user loop works — she adds, he sees and acts — and that no functionality regressed on either platform.
Output: Confirmed working app on both devices, Phase 3 complete.
</objective>

<execution_context>
@/Users/anish/Developer/Personal/LibM/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anish/Developer/Personal/LibM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/anish/Developer/Personal/LibM/.planning/phases/03-wishlist-detail/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify wishlist, detail view, and cross-platform parity on both devices</name>
  <files>N/A — checkpoint only, no files modified</files>
  <action>Human verification step. Run the app on both physical devices and follow the how-to-verify flows below.</action>
  <what-built>
    Complete Phase 3 feature set:
    - WishlistScreen: shelf-style display, her-only add button, empty state, error state
    - BookDetailScreen: cover swatch, title, author, notes, context-aware Move to Library button
    - Move-to-library action: removes book from wishlist, adds to shelf end
    - All spine taps on both Library and Wishlist open detail view
    - Cross-platform: app builds and runs on iOS and Android
  </what-built>
  <how-to-verify>
    Run the app on her Android device and his iOS device. Verify all flows:

    **Flow 1 — Wishlist add (her Android):**
    1. Log in as her account on Android
    2. Navigate to Wishlist screen (via wishlist icon in Library app bar)
    3. Verify: empty state shows "Her wishlist is empty" in italic muted text
    4. Tap '+' — AddBookScreen opens with title "Add to Wishlist"
    5. Enter a title and author, pick a cover color, tap Save
    6. Verify: returned to Wishlist, new book appears as a spine on the shelf

    **Flow 2 — Wishlist view (his iOS):**
    1. Log in as his account on iOS (or refresh if already logged in)
    2. Navigate to Wishlist screen
    3. Verify: the book she added is visible on the shelf
    4. Verify: NO '+' add button visible in the app bar (only the logout icon)

    **Flow 3 — Move to Library (his iOS):**
    1. On his device, tap the wishlist book spine
    2. Verify: BookDetailScreen opens showing cover color block, title, author
    3. Verify: "Move to Library" button is visible (Burgundy color)
    4. Tap "Move to Library"
    5. Verify: navigated back to WishlistScreen; the book is gone from wishlist
    6. Navigate to Library — verify the book appears at the end of the shelf

    **Flow 4 — Library book detail (either device):**
    1. Navigate to Library screen
    2. Tap any existing shelf book spine
    3. Verify: BookDetailScreen opens showing all its fields
    4. Verify: NO "Move to Library" button visible (library book only)
    5. Tap back arrow — verify returned to Library

    **Flow 5 — Her wishlist add button not shown to him:**
    1. On his iOS device, on Wishlist screen
    2. Verify: app bar shows only the logout icon (and any navigation icons), not the '+' add icon

    **Regression check:**
    - Library drag-reorder still works
    - Login/logout still works on both devices
    - Adding a shelf book via Library '+' still works (is_wishlist=false)
  </how-to-verify>
  <verify>Human approves all flows above — no automated command applies to physical device testing.</verify>
  <done>All 5 flows pass on both physical devices with no regressions.</done>
  <resume-signal>Type "approved" if all flows pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval of all 5 flows on both physical devices.
</verification>

<success_criteria>
- All 5 verification flows pass on both devices
- No regressions in auth, library, or add-book flows
- Phase 3 complete: WISH-01, WISH-02, BOOK-01, PLAT-01 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-wishlist-detail/03-04-SUMMARY.md`
</output>
